<model id="{{id}}" parent="{{parentId}}" dataTable="{{dataTable}}" virtual="{{virtual}}">
	{{#fields}}
	<field id="{{id}}" dataType="{{dataType}}" {{#primaryKey}}primaryKey="TRUE"{{/primaryKey}} {{#alias}}alias="{{alias}}"{{/alias}}/>
	{{#fields}}
</model>

