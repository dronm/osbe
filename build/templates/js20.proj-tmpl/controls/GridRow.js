/* Copyright (c) 2016	Andrey Mikhalevich, Katren ltd.*//*		Description*//** Requirements * @requires core/extend.js * @requires controls/ControlContainer.js*//* constructor */function GridRow(id,options){	options = options || {};		GridRow.superclass.constructor.call(this, id, (options.tagName || this.DEF_TAG_NAME), options);}extend(GridRow,ControlContainer);GridRow.prototype.DEF_TAG_NAME = "tr";GridRow.prototype.m_initColumns;GridRow.prototype.setInitColumnOrder = function(){	//this.m_initColumns = CommonHelper.clone(this.m_elements);		this.m_initColumns = {};	for (var id in this.m_elements){		this.m_initColumns[id] = CommonHelper.clone(this.m_elements[id]);	}	}GridRow.prototype.setColumnOrder = function(orderArray){		this.m_elements = {};		for (var i=0;i<orderArray.length;i++){		if (!orderArray[i].checked)continue;				if (this.m_initColumns[orderArray[i].colId]){			this.m_elements[orderArray[i].colId] = this.m_initColumns[orderArray[i].colId];		}		/*		for (var id in this.m_initColumns){			if (this.m_initColumns[id].getName()==orderArray[i].colId){				this.m_elements[id] = this.m_initColumns[id];				break;			}		}		*/	}	/*	for (var id in this.m_initColumns){		if (!this.m_initColumns[id].getVisible() && !orderArray[this.m_initColumns[id].getName()]){						this.m_elements[id] = this.m_initColumns[id];		}	}	*/}GridRow.prototype.getInitColumns = function(){	return this.m_initColumns;}